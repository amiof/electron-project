{
  "name": "downloader",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start:react\"  \"npm run start:electron\"",
    "start:react": "cd react && npm run dev",
    "start:electron": "cd electron && npm run start",
    "build": "npm run build:electron && npm run build:react",
    "build:react": "cd react && npm run build",
    "build:electron": "cd electron && npm run build",
    "package": "npm run build && electron-builder",
    "package:win": "npm run build && electron-builder --win",
    "icon": "electron-icon-builder --input=/assets/icon_1024x1024.png --output=./build --flatten"
  },
  "build": {
    "appId": "com.yourapp.id",
    "directories": {
      "app": "electron",
      "output": "release",
      "buildResources": "assets"
    },
    "files": [
      "**/*"
    ],
    "icon": "assets",
    "extraResources": [
      {
        "from": "react/dist",
        "to": "react/dist",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "electron/src/release-aria2/",
        "to": "electron/dist",
        "filter": [
          "**/*"
        ]
      }
    ],
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "identity": null,
      "icon": "icon"
    },
    "dmg": {
      "writeUpdateInfo": true
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "sign": false,
      "icon": "icon",
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "differentialPackage": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "runAfterFinish": true,
      "shortcutName": "AMDownloader",
      "artifactName": "${productName}-${version}-Setup.${ext}"
    },
    "portable": {
      "artifactName": "${productName}-${version}-Portable.${ext}"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb",
        "rpm",
        "tar.xz"
      ]
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "electron": "^34.0.2",
    "electron-builder": "^25.1.8",
    "electron-icon-builder": "^2.0.1"
  }
}
